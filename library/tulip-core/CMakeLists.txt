IF(WIN32)
  SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -DDLL_TULIP")
ENDIF(WIN32)
  SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -DTULIP_INSTALL_LIBDIR=\"${CMAKE_INSTALL_LIBDIR}\"")

ADD_CORE_FILES(.)
ADD_SUBDIRECTORY(include)
ADD_SUBDIRECTORY(src)

