ADD_CORE_FILES(CMakeLists.txt)

IF(EMSCRIPTEN)
ADD_SUBDIRECTORY(tulip-javascript)
ENDIF(EMSCRIPTEN)

ADD_SUBDIRECTORY(tulip-core)
ADD_SUBDIRECTORY(tulip-ogdf)

IF(NOT BUILD_CORE_ONLY OR EMSCRIPTEN)
ADD_SUBDIRECTORY(tulip-ogles)
ENDIF(NOT BUILD_CORE_ONLY OR EMSCRIPTEN)

IF(NOT BUILD_CORE_ONLY)
ADD_SUBDIRECTORY(tulip-gui)
IF(BUILD_PYTHON_COMPONENTS)
ADD_SUBDIRECTORY(tulip-python)
ENDIF(BUILD_PYTHON_COMPONENTS)
ENDIF(NOT BUILD_CORE_ONLY)


