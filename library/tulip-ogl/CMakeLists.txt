IF(TULIP_CHECK_MEMORY)
  SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${TULIP_CHECK_MEMORY_FLAGS}")
ENDIF(TULIP_CHECK_MEMORY)

IF(WIN32)
   SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -DDLL_TULIP_GL")
ENDIF(WIN32)

ADD_SUBDIRECTORY(include)
ADD_SUBDIRECTORY(bitmaps)
ADD_SUBDIRECTORY(src)
