INCLUDE_DIRECTORIES(${QT_INCLUDE_DIR})
ADD_EXECUTABLE(tulip_launcher main.cpp)
TARGET_LINK_LIBRARIES(tulip_launcher ${QT_QTCORE_LIBRARY})
SET_TARGET_PROPERTIES(tulip PROPERTIES INSTALL_RPATH "$ORIGIN/tulip/lib")
INSTALL(TARGETS tulip_launcher DESTINATION macos COMPONENT macos)
