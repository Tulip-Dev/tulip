INCLUDE_DIRECTORIES(${TulipCoreInclude} ${TulipCoreBuildInclude})
FILE(GLOB LIB_SRCS "*.cpp")

ADD_LIBRARY(Grip-${TULIP_VERSION} SHARED ${LIB_SRCS})
TARGET_LINK_LIBRARIES(Grip-${TULIP_VERSION} ${TULIP_CORE_LIBRARY})

INSTALL(TARGETS Grip-${TULIP_VERSION} DESTINATION ${TulipPluginsInstallDir} COMPONENT Grip)
