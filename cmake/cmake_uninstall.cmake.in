if(NOT EXISTS "@CMAKE_CURRENT_BINARY_DIR@/install_manifest.txt")
    message(FATAL_ERROR "Cannot find install manifest: \"@CMAKE_CURRENT_BINARY_DIR@/install_manifest.txt\"")
endif(NOT EXISTS "@CMAKE_CURRENT_BINARY_DIR@/install_manifest.txt")

file(READ "@CMAKE_CURRENT_BINARY_DIR@/install_manifest.txt" files)
string(REGEX REPLACE "\n" ";" files "${files}")
list(REVERSE files)
foreach (file ${files})
    message(STATUS "Uninstalling \"$ENV{DESTDIR}${file}\"")
    if (EXISTS "$ENV{DESTDIR}${file}")
        execute_process(
            COMMAND @CMAKE_COMMAND@ -E remove "$ENV{DESTDIR}${file}"
            OUTPUT_VARIABLE rm_out
            RESULT_VARIABLE rm_retval
        )
        if(NOT ${rm_retval} EQUAL 0)
            message(FATAL_ERROR "Problem when removing \"$ENV{DESTDIR}${file}\"")
        endif (NOT ${rm_retval} EQUAL 0)
    else (EXISTS "$ENV{DESTDIR}${file}")
        message(STATUS "File \"$ENV{DESTDIR}${file}\" does not exist.")
    endif (EXISTS "$ENV{DESTDIR}${file}")
endforeach(file)

# also remove desktop file and file associations on Linux
if (NOT WIN32 AND NOT APPLE)
  execute_process(COMMAND @CMAKE_COMMAND@ -E remove "$ENV{HOME}/.local/share/mime/packages/application-x-tulip-tlpx.xml" OUTPUT_QUIET ERROR_QUIET)
  execute_process(COMMAND @CMAKE_COMMAND@ -E remove "$ENV{HOME}/.local/share/mime/packages/application-x-tulip-tlp.xml" OUTPUT_QUIET ERROR_QUIET)
  execute_process(COMMAND @CMAKE_COMMAND@ -E remove "$ENV{HOME}/.local/share/mime/packages/application-x-tulip-tlpgz.xml" OUTPUT_QUIET ERROR_QUIET)
  execute_process(COMMAND @CMAKE_COMMAND@ -E remove "$ENV{HOME}/.local/share/mime/packages/application-x-tulip-tlpz.xml" OUTPUT_QUIET ERROR_QUIET)
  execute_process(COMMAND @CMAKE_COMMAND@ -E remove "$ENV{HOME}/.local/share/mime/packages/application-x-tulip-tlpb.xml" OUTPUT_QUIET ERROR_QUIET)
  execute_process(COMMAND @CMAKE_COMMAND@ -E remove "$ENV{HOME}/.local/share/mime/packages/application-x-tulip-tlpbgz.xml" OUTPUT_QUIET ERROR_QUIET)
  execute_process(COMMAND @CMAKE_COMMAND@ -E remove "$ENV{HOME}/.local/share/mime/packages/application-x-tulip-tlpbz.xml" OUTPUT_QUIET ERROR_QUIET)
  execute_process(COMMAND @CMAKE_COMMAND@ -E remove "$ENV{HOME}/.local/share/applications/Tulip.desktop" OUTPUT_QUIET ERROR_QUIET)
  execute_process(COMMAND @CMAKE_COMMAND@ -E remove "$ENV{HOME}/.local/share/pixmaps/tulip.png" OUTPUT_QUIET ERROR_QUIET)
  execute_process(COMMAND @CMAKE_COMMAND@ -E remove "$ENV{HOME}/.local/share/pixmaps/tulip-file-icon.png" OUTPUT_QUIET ERROR_QUIET)
  execute_process(COMMAND @CMAKE_COMMAND@ -E remove "$ENV{HOME}/.local/share/icons/tulip.png" OUTPUT_QUIET ERROR_QUIET)
  execute_process(COMMAND @CMAKE_COMMAND@ -E remove "$ENV{HOME}/.local/share/icons/tulip-file-icon.png" OUTPUT_QUIET ERROR_QUIET)
  execute_process(COMMAND update-desktop-database "$ENV{HOME}/.local/share/applications" OUTPUT_QUIET ERROR_QUIET)
  execute_process(COMMAND update-mime-database "$ENV{HOME}/.local/share/mime" OUTPUT_QUIET ERROR_QUIET)
endif (NOT WIN32 AND NOT APPLE)
