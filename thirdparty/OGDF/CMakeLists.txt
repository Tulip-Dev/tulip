include_directories(${CMAKE_CURRENT_SOURCE_DIR})

FILE(GLOB_RECURSE OGDF_SOURCES "src/*/*.cpp")

ADD_LIBRARY(${OGDF_LIBRARY} STATIC ${OGDF_SOURCES} )
IF(WIN32)
TARGET_LINK_LIBRARIES(${OGDF_LIBRARY} psapi)
ENDIF(WIN32)