SET(gzstream_SRCS
    gzstream.cpp 
    gzstream.h
)

INCLUDE_DIRECTORIES(${ZLIB_INCLUDE_DIR} ${CMAKE_CURRENT_SOURCE_DIR})

IF(UNIX)
SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fPIC")
ENDIF(UNIX)

ADD_LIBRARY(gzstream STATIC ${gzstream_SRCS})
TARGET_LINK_LIBRARIES(gzstream ${ZLIB_LIBRARY})
